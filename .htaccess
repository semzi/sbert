RewriteEngine On

# Serve existing files/directories as‑is
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# All other requests -> index.html (React Router SPA)
RewriteRule ^ index.html [L]
